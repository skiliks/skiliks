/*global Backbone:false, console, SKApp, session */

(function () {
    "use strict";
    window.SKPhoneHistory = Backbone.Model.extend({
        defaults: {
            is_read:false
        }
    });
})();