/*global Backbone, define */

define([], function () {
    "use strict";

    /**
     * @class SKMeetingSubject
     * @augments Backbone.Model
     */
    window.SKMeetingSubject = Backbone.Model.extend({

    });

    return window.SKMeetingSubject;
});